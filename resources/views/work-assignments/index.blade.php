@extends('layouts.app', ['title' => 'Work Assignment'])
@section('content')
<div class="mb-6 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><h2 class="text-2xl font-bold">Work Assignment</h2><p class="mt-1 text-sm text-slate-500">Schedule technicians against open service requests.</p></div><a href="{{ route('assignments.create') }}" class="btn-primary">+ Assign Work</a></div>
<form class="card mb-5 grid gap-3 p-4 md:grid-cols-[1fr_240px_auto]"><input name="search" value="{{ request('search') }}" class="form-input" placeholder="Assignment, request or technician"><select name="status" class="form-input"><option value="">All statuses</option>@foreach(['scheduled','in_progress','completed','cancelled'] as $status)<option value="{{ $status }}" @selected(request('status') === $status)>{{ str($status)->replace('_',' ')->title() }}</option>@endforeach</select><button class="btn-primary justify-center">Filter</button></form>
<div class="card overflow-x-auto"><table class="w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase text-slate-500"><tr><th class="p-4">Assignment</th><th class="p-4">Service Request</th><th class="p-4">Technician</th><th class="p-4">Schedule</th><th class="p-4">Priority</th><th class="p-4">Status</th><th class="p-4 text-right">Actions</th></tr></thead><tbody class="divide-y">
@forelse($records as $record)<tr><td class="p-4"><a href="{{ route('assignments.show',$record) }}" class="font-semibold text-blue-600">{{ $record->assignment_code }}</a><div class="text-xs text-slate-400">{{ ucfirst($record->assignment_role) }}</div></td><td class="p-4">{{ $record->serviceRequest->request_code }}<div class="text-xs text-slate-400">{{ $record->serviceRequest->customer->customer_name }} · {{ $record->serviceRequest->machine?->machine_name }}</div></td><td class="p-4 font-medium">{{ $record->technician->name }}<div class="text-xs text-slate-400">{{ $record->technician->employee_code }}{{ $record->skill ? ' · '.$record->skill->name : '' }}</div></td><td class="p-4">{{ $record->scheduled_date->format('d M Y') }}<div class="text-xs text-slate-400">{{ date('g:i A',strtotime($record->start_time)) }} – {{ date('g:i A',strtotime($record->end_time)) }}</div></td><td class="p-4">{{ ucfirst($record->priority) }}</td><td class="p-4"><span class="status-badge {{ $record->status === 'completed' ? 'status-success' : 'status-muted' }}">{{ str($record->status)->replace('_',' ')->title() }}</span></td><td class="p-4"><div class="flex justify-end gap-2"><a class="table-action" href="{{ route('assignments.edit',$record) }}">Edit</a><form method="POST" action="{{ route('assignments.destroy',$record) }}">@csrf @method('DELETE')<button class="table-action text-rose-600">Delete</button></form></div></td></tr>
@empty<tr><td colspan="7" class="p-12 text-center text-slate-400">No work assignments found.</td></tr>@endforelse</tbody></table><div class="p-4">{{ $records->links() }}</div></div>
@endsection
