@extends('layouts.app',['title'=>'Service Reports'])
@section('content')<div class="mb-6 flex justify-between"><div><h2 class="text-2xl font-bold">Service Reports</h2><p class="text-sm text-slate-500">Complete service lifecycle from request creation to closure.</p></div></div><form class="card mb-5 flex gap-3 p-4"><input name="search" value="{{ request('search') }}" class="form-input" placeholder="Search request ID or customer"><button class="btn-primary">Search</button></form><div class="card overflow-x-auto"><table class="w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase text-slate-500"><tr><th class="p-4">Request</th><th class="p-4">Customer / Machine</th><th class="p-4">Created</th><th class="p-4">Assignments</th><th class="p-4">Status</th><th class="p-4">Report</th></tr></thead><tbody class="divide-y">@forelse($records as $record)<tr><td class="p-4 font-semibold text-blue-600">{{ $record->request_code }}</td><td class="p-4">{{ $record->customer?->customer_name ?? 'Deleted customer' }}<div class="text-xs text-slate-400">{{ $record->machine?->machine_name ?? $record->product_name }}</div></td><td class="p-4">{{ $record->created_at->format('d M Y, g:i A') }}</td><td class="p-4">{{ $record->work_assignments_count }}</td><td class="p-4">{{ str($record->status)->replace('_',' ')->title() }}</td><td class="p-4"><a href="{{ route('service-reports.show',$record) }}" class="table-action text-blue-600">View lifecycle</a></td></tr>@empty<tr><td colspan="6" class="p-12 text-center text-slate-400">No service reports found.</td></tr>@endforelse</tbody></table><div class="p-4">{{ $records->links() }}</div></div>@endsection
