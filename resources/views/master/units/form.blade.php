@extends('layouts.app',['title'=>$unit->exists?'Edit Unit':'Add Unit']) @section('content')<form method="POST" action="{{$unit->exists?route('units.update',$unit):route('units.store')}}" class="mx-auto max-w-xl">@csrf @if($unit->exists)@method('PUT')@endif<div class="card p-7"><h2 class="text-2xl font-bold">{{$unit->exists?'Edit':'Add'}} Unit</h2><div class="mt-6">@include('master._field',['model'=>$unit,'name'=>'unit_name','label'=>'Unit Name','required'=>true])</div><div class="mt-7 flex justify-end gap-3"><a href="{{route('units.index')}}" class="btn-secondary">Cancel</a><button class="btn-primary">Save Unit</button></div></div></form>@endsection
