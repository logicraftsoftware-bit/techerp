@extends('layouts.app',['title'=>$role->exists?'Edit Role':'Add Role']) @section('content')<form method="POST" action="{{$role->exists?route('roles.update',$role):route('roles.store')}}" class="mx-auto max-w-xl">@csrf @if($role->exists)@method('PUT')@endif<div class="card p-7"><h2 class="text-2xl font-bold">{{$role->exists?'Edit':'Add'}} Role</h2><div class="mt-6">@include('master._field',['model'=>$role,'name'=>'name','label'=>'Role Name','required'=>true])</div><div class="mt-7 flex justify-end gap-3"><a href="{{route('roles.index')}}" class="btn-secondary">Cancel</a><button class="btn-primary">Save Role</button></div></div></form>@endsection
