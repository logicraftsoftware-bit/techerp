<div class="mb-6 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p class="text-xs font-semibold uppercase tracking-[.2em] text-blue-600">Master Data</p><h2 class="mt-2 text-3xl font-bold text-slate-900">{{ $heading }}</h2><p class="mt-1 text-sm text-slate-500">{{ $description }}</p></div><div class="flex gap-3">{!! $actions ?? '' !!}<a href="{{ $create }}" class="btn-primary">+ Add {{ $singular }}</a></div></div>
<form class="card mb-5 flex gap-3 p-4"><input class="form-input max-w-md" name="search" value="{{ request('search') }}" placeholder="Search {{ strtolower($heading) }}..."><button class="btn-secondary">Search</button>@if(request('search'))<a href="{{ url()->current() }}" class="btn-secondary">Clear</a>@endif</form>
