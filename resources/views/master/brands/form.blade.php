@extends('layouts.app',['title'=>$brand->exists?'Edit Brand':'Add Brand']) @section('content')<form method="POST" action="{{$brand->exists?route('brands.update',$brand):route('brands.store')}}" class="mx-auto max-w-xl">@csrf @if($brand->exists)@method('PUT')@endif<div class="card p-7"><h2 class="text-2xl font-bold">{{$brand->exists?'Edit':'Add'}} Brand</h2><div class="mt-6">@include('master._field',['model'=>$brand,'name'=>'brand_name','label'=>'Brand Name','required'=>true])</div><div class="mt-7 flex justify-end gap-3"><a href="{{route('brands.index')}}" class="btn-secondary">Cancel</a><button class="btn-primary">Save Brand</button></div></div></form>@endsection
