@extends('layouts.app',['title'=>'Parts Used in Job']) @section('content')<h2 class="text-2xl font-bold">Parts Used in Job</h2><p class="mb-5 text-sm text-slate-500">Work-wise consumption and automatic stock deduction.</p><form method="POST" action="{{route('job-parts.store')}}" class="card mb-6 grid gap-3 p-5 md:grid-cols-4">@csrf<select name="work_assignment_id" class="form-input" required><option value="">Work ID</option>@foreach($assignments as $x)<option value="{{$x->id}}">{{$x->assignment_code}}</option>@endforeach</select><select name="part_id" class="form-input" required><option value="">Part</option>@foreach($parts as $x)<option value="{{$x->id}}">{{$x->part_name}} ({{$x->current_stock}})</option>@endforeach</select><input type="number" name="quantity" min="1" class="form-input" placeholder="Quantity" required><input type="number" step=".01" name="rate" class="form-input" placeholder="Rate" required><input type="number" step=".01" name="tax_percent" value="0" class="form-input" placeholder="Tax %"><input name="serial_number" class="form-input" placeholder="Serial number"><label class="form-input"><input type="checkbox" name="under_warranty" value="1"> Under warranty</label><button class="btn-primary justify-center">Add Consumption</button></form>@include('parts-inventory._table',['headers'=>['Work ID','Part','Qty','Rate','Tax','Total'],'rows'=>$records->map(fn($x)=>[$x->assignment->assignment_code,$x->part->part_name,$x->quantity,$x->rate,$x->tax_percent,number_format($x->quantity*$x->rate*(1+$x->tax_percent/100),2)])])@endsection
