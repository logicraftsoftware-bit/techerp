@extends('layouts.app',['title'=>'Parts Used in Job']) @section('content')<div class="mb-6 flex items-end justify-between gap-4"><div><h2 class="text-2xl font-bold">Parts Used in Job</h2><p class="mt-1 text-sm text-slate-500">Work-wise consumption and automatic stock deduction.</p></div><a href="{{route('job-parts.create')}}" class="btn-primary">+ Add Consumption</a></div>@include('parts-inventory._table',['headers'=>['Work ID','Part','Qty','Rate','Tax','Total'],'rows'=>$records->map(fn($x)=>[$x->assignment->assignment_code,$x->part->part_name,$x->quantity,$x->rate,$x->tax_percent,number_format($x->quantity*$x->rate*(1+$x->tax_percent/100),2)])])@endsection
