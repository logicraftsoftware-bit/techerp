<div class="card overflow-x-auto"><table class="w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase text-slate-500"><tr>@foreach($headers as $h)<th class="p-4">{{$h}}</th>@endforeach</tr></thead><tbody class="divide-y">@forelse($rows as $row)<tr>@foreach($row as $v)<td class="p-4">{{$v}}</td>@endforeach</tr>@empty<tr><td colspan="{{count($headers)}}" class="p-12 text-center text-slate-400">No records yet.</td></tr>@endforelse</tbody></table><div class="p-4">{{$records->links()}}</div></div>
