@extends('layouts.app',['title'=>'Attendance']) @section('content')
<div class="mb-5 flex flex-wrap items-end justify-between gap-3"><div><h2 class="text-2xl font-bold">Staff Attendance</h2><p class="text-sm text-slate-500">Daily attendance, timing, overtime and remarks for technicians and CRM users.</p></div><form><label class="form-label">Attendance Date</label><input class="form-input" type="date" name="date" value="{{ $date->format('Y-m-d') }}" onchange="this.form.submit()"></form></div>
<form method="POST" action="{{route('attendance.store')}}" class="card overflow-hidden">@csrf<input type="hidden" name="attendance_date" value="{{$date->format('Y-m-d')}}"><div class="overflow-x-auto"><table class="w-full text-sm"><thead class="bg-slate-50 text-left"><tr><th class="p-4">Staff</th><th>Status</th><th>Check In</th><th>Check Out</th><th>Overtime</th><th>Remarks</th></tr></thead><tbody class="divide-y">@foreach($staff as $s) @php($row=$records->get($s->key))<tr><td class="p-4 font-semibold">{{$s->code}}<br><span class="font-normal text-slate-500">{{$s->name}}</span></td><td><select name="attendance[{{$s->key}}][status]" class="form-input min-w-32">@foreach(['present','absent','leave','half_day','weekly_off'] as $st)<option value="{{$st}}" @selected(($row?->attendance_status ?? 'present')===$st)>{{str($st)->replace('_',' ')->title()}}</option>@endforeach</select></td><td><input type="time" name="attendance[{{$s->key}}][check_in]" value="{{$row?->check_in}}" class="form-input"></td><td><input type="time" name="attendance[{{$s->key}}][check_out]" value="{{$row?->check_out}}" class="form-input"></td><td><input type="number" step=".5" min="0" name="attendance[{{$s->key}}][overtime_hours]" value="{{$row?->overtime_hours ?? 0}}" class="form-input w-24"></td><td><input name="attendance[{{$s->key}}][remarks]" value="{{$row?->remarks}}" class="form-input"></td></tr>@endforeach</tbody></table></div><div class="flex justify-end border-t p-4"><button class="btn-primary">Save Attendance</button></div></form>
@endsection
