@csrf @if($user->exists) @method('PUT') @endif
<div class="grid gap-5 md:grid-cols-2"><div><label class="form-label">Full name *</label><input name="name" class="form-input" value="{{ old('name',$user->name) }}" required></div><div><label class="form-label">Email *</label><input name="email" type="email" class="form-input" value="{{ old('email',$user->email) }}" required></div><div><label class="form-label">Phone</label><input name="phone" class="form-input" value="{{ old('phone',$user->phone) }}"></div><div><label class="form-label">Account status</label><label class="mt-3 flex items-center gap-2 text-sm"><input type="checkbox" name="is_active" value="1" @checked(old('is_active',$user->exists ? $user->is_active : true)) class="rounded"> Active and able to sign in</label></div><div><label class="form-label">Password {{ $user->exists ? '(leave blank to keep current)' : '*' }}</label><input name="password" type="password" class="form-input" {{ $user->exists ? '' : 'required' }}></div><div><label class="form-label">Confirm password</label><input name="password_confirmation" type="password" class="form-input"></div></div>
<fieldset class="mt-7"><legend class="form-label">Assigned roles *</legend><div class="mt-2 grid gap-3 sm:grid-cols-2 xl:grid-cols-3">@foreach($roles as $role)<label class="flex cursor-pointer items-start gap-3 rounded-xl border border-slate-200 p-4 hover:border-blue-300"><input type="checkbox" name="roles[]" value="{{ $role->id }}" class="mt-1 rounded" @checked(in_array($role->id, old('roles',$user->roles->pluck('id')->all())))><span><span class="block text-sm font-semibold text-slate-800">{{ $role->name }}</span><span class="text-xs text-slate-400">{{ $role->description }}</span></span></label>@endforeach</div></fieldset>
<div class="mt-8 flex justify-end gap-3"><a href="{{ route('users.index') }}" class="btn-secondary">Cancel</a><button class="btn-primary">{{ $user->exists ? 'Save changes' : 'Create user' }}</button></div>
