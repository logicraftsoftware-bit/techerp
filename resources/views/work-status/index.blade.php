@extends('layouts.app',['title'=>'Work Status'])
@section('content')<div class="mb-6 flex justify-between"><div><h2 class="text-2xl font-bold">Work Status</h2><p class="text-sm text-slate-500">Track every Work ID and its current state.</p></div><form><select name="status" class="form-input" onchange="this.form.submit()"><option value="">All statuses</option>@foreach(['scheduled','in_progress','completed','cancelled'] as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ str($status)->replace('_',' ')->title() }}</option>@endforeach</select></form></div><div class="card overflow-x-auto"><table class="w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase text-slate-500"><tr><th class="p-4">Work ID</th><th class="p-4">Customer</th><th class="p-4">Technician</th><th class="p-4">Schedule</th><th class="p-4">Current Status</th></tr></thead><tbody class="divide-y">@forelse($records as $record)<tr><td class="p-4"><a class="font-semibold text-blue-600" href="{{ route('work-status.show',$record) }}">{{ $record->assignment_code }}</a></td><td class="p-4">{{ $record->serviceRequest->customer?->customer_name ?? 'Deleted customer' }}</td><td class="p-4">{{ $record->technician->name }}</td><td class="p-4">{{ $record->scheduled_date->format('d M Y') }}</td><td class="p-4"><span class="status-badge status-muted">{{ str($record->status)->replace('_',' ')->title() }}</span></td></tr>@empty<tr><td colspan="5" class="p-12 text-center text-slate-400">No work found.</td></tr>@endforelse</tbody></table><div class="p-4">{{ $records->links() }}</div></div>@endsection
