@extends('layouts.app',['title'=>'Machine Inventory']) @section('content')<div class="mb-6 flex items-end justify-between gap-4"><div><h2 class="text-2xl font-bold">Machine Inventory</h2><p class="mt-1 text-sm text-slate-500">Stock in, stock out and adjustments with running balance for machines.</p></div><a href="{{route('machine-inventory.create')}}" class="btn-primary">+ Add Transaction</a></div>@include('parts-inventory._table',['headers'=>['Date','Machine','Type','Qty','Balance','Reference'],'rows'=>$records->map(fn($x)=>[$x->created_at->format('d M Y'),$x->machine->machine_name,str($x->transaction_type)->replace('_',' ')->title(),$x->quantity,$x->balance_after,$x->reference])])@endsection
